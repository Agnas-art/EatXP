import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.animeeats.academy',
  appName: 'Anime Eats Academy',
  webDir: 'dist'
};

export default config;
